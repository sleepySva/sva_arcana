function init()
   effect.addStatModifierGroup({{stat = "fireResistance", amount = 0.0}, {stat = "nemesisMusicStatusImmunity", amount = 1}})

   script.setUpdateDelta(0)
end

function update(dt)

end

function uninit()
  
end
